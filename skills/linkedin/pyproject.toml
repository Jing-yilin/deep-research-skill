[project]
name = "linkedin-skill"
version = "0.1.0"
description = "LinkedIn skill via HarvestAPI (read) and Composio (write)"
requires-python = ">=3.12"
dependencies = [
    "composio>=0.11.0",
    "requests>=2.31.0",
    "toon_format @ git+https://github.com/toon-format/toon-python.git",
]
